# Stickerbox

This bit of code controlls LEDs on a Stickerbox

#How to add an anomation

Any animation is displayed for 1000 frames.

There are methods in modes.h which should end in "mode" and get the number of
the frame as a parameter. The can also call an array leds which contains RGB
structs. After each call of the mode-method the display is changed to whatever
is set in this led array.
